[package]
name = "soulcast"
version = "0.1.0"
description = "SoulCast Anchor Smart Contract"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "soulcast"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []
init-if-needed = ["anchor-lang/init-if-needed"]

[dependencies]
anchor-lang = { version = "0.30.1", features = ["init-if-needed"] }
anchor-spl = "0.30.1"
